{
  "name": "crm-lead-form-desktop-app",
  "version": "1.0.0",
  "description": "Desktop application with React frontend and FastAPI backend for CRM lead form management",
  "scripts": {
    "install-all": "cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "start-backend": "cd backend && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start-frontend": "cd frontend && npm run dev",
    "test-api": "powershell -ExecutionPolicy Bypass -File ./test-api.ps1",
    "build-frontend": "cd frontend && npm run build"
  },
  "keywords": [
    "crm",
    "lead-form",
    "react",
    "fastapi",
    "google-sheets",
    "excel"
  ],
  "author": "",
  "license": "ISC"
}
